let qntCasas = 0;
let jpgoIniciado = false;
let tiros = 0;
let acertos = 0;

funcion createBatleField(qnt) {
  qntCasas = qnt;
    if (!jogoIniciado) {
      document.body.appendChild(document.createElement('br'));
        for (let x = 0; x < qnt; x++) {
          for (let y = 0; y < qnt; y++) {
            let itemId = 'div$(x)$(y)';
            let newDiv = document.createElement('div');
            newDiv.classeName = 'agua' ;
            newDiv.
            newDiv.
            newDiv.
            newDiv.
            newDiv.
            newDiv.
            newDiv.
            newDiv.
          }
        }
    }
}
